# equip-claude-code

Claude Code plugin that analyzes projects and installs tailored agents, skills, commands, hooks, MCP servers, and project rules from community registries.

## Development

- Follow existing file structure and naming conventions.
- Skills are Markdown files with YAML frontmatter — see `skills/` for examples.
- Test changes by running `/equip` in a sample project.

# currentDate
Today's date is 2026-02-16.
